// operador módulo é representado por % e retorna o resto da divisão

// ex exibir na tela todos os numeros pares entre 0 e 20

var num = 0;
while(num <= 20) {
    num % 2 === 0 ? console.log(num): '';
    num++;
}